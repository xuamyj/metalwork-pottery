using UnityEngine;

public class WorldNavId : MonoBehaviour
{
    public WorldStatus worldNavId;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
